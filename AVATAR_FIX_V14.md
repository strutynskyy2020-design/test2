# Avatar fixes V14

- Removed the dark camera strip from the profile photo; the full image is now visible.
- Added `avatar_url` to feed and leaderboard API payloads.
- Feed and leaderboard now render uploaded profile photos with initials as fallback.
- Bumped PWA service-worker registration version to force an update.
