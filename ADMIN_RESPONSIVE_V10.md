# Admin Daily Tasks V10

- Mobile/tablet keeps the existing TM6 Bonus card interface.
- Laptop/desktop (Tailwind `lg`, from 1024px) switches to a wide operator table.
- Admin route expands to 1500px on desktop; other application pages keep the 480px mobile shell.
- Desktop admin uses a left navigation panel and hides the bottom mobile navigation.
- Approve/reject logic and backend endpoints are unchanged.
